Ты работаешь в проекте saga (Transactional Outbox + CDC). Правила проекта описаны в `saga/CLAUDE.md` — прочитай его перед тем, как начинать.

Задача: $ARGUMENTS

Ответь строго в следующем формате (он описан в разделе «2. Требования к результату» файла CLAUDE.md):

---

## План действий

Нумерованный список шагов до начала любого кода.

> Если задача затрагивает Outbox-транзакцию, CDC-конфигурацию или контракт между сервисами — задай уточняющий вопрос перед тем, как продолжить.

---

## Изменения в коде

Для каждого изменяемого файла:
- путь к файлу относительно `saga/`
- полный изменённый фрагмент (не diff, а готовый код)
- если файл создаётся — полное содержимое

---

## Пояснение решений

Один абзац: почему выбран именно такой подход, какие альтернативы рассматривались и почему отклонены.

---

## Шаги проверки

Конкретные `curl`- или `mvn`-команды с ожидаемым результатом. Например:

```bash
curl -X POST http://localhost:8080/v1/...
# Ожидаемый результат: ...
```

---

Перед отправкой ответа сверь решение с ограничениями из раздела «1. Контекст и границы» файла `saga/CLAUDE.md`.
